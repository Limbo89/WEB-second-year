<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title> Шаблонизация
        {{!-- {{#if title}}
            {{{title}}}
        {{else}}
        Название?
        {{/if}} --}}
    </title>
    
</head>
<body>
    {{!-- <h1>Здравствуй {{{ surname }}} {{{ name }}}, твой возвраст: {{{ age }}}, а рост: {{{ height }}}.</h1>
    <img src="{{{img}}}" alt="картинки нету">
    <p>Максу хватает только на:</p>
    <ul>
        {{#each list}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
    <ul>
        {{#each keys}}
        <li>{{@key}} - {{this}}</li>
        {{/each}}
    </ul> --}}
    <div>
        <img src="" alt="">
        {{#each chapters}}
        <img src="{{this.Картинка}}" alt="404 ERROR">
        <p>{{this.Имя}}</p>
        <p>{{this.Регион}}</p>
        <p>{{this.Описание}}</p>
        {{/each}}
    </div>
</body>
</html>